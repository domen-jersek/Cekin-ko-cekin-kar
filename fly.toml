# Fly.io app config. Replace "cekincki-bot" with your app name.
app = "cekincki-bot"
primary_region = "ams"
kill_signal = "SIGINT"
kill_timeout = "5s"

[env]
  NODE_ENV = "production"

# This is a worker (no HTTP). Do not configure http_service.
[processes]
  app = "npm start"
